# ESP32-S3 16MB flash partition table
# No OTA — single factory app, plus a small SPIFFS partition for the
# playlist/config (max ~255 stations, a text file of that size is tiny -
# no need to reserve most of the flash for it)
#
# Flash size: 0x1000000 (16MB) - only ~3.3MB of it is partitioned below;
# the rest is deliberately left unallocated for now.
# Bootloader: 0x0000 – 0x8FFF (first 36KB, managed by IDF)
#
# Name,     Type,  SubType,  Offset,   Size,    Flags
nvs,        data,  nvs,      0x9000,   0x6000,
phy_init,   data,  phy,      0xF000,   0x1000,
factory,    app,   factory,  0x10000,  0x300000,
storage,    data,  spiffs,   0x310000, 0x100000,
